{
  "compilerOptions": {
    "baseUrl": ".",                     // ✅ Required for `paths` to work
    "paths": {
      "@/*": ["./*"]                    // ✅ Makes `@/lib/...` resolve correctly
    }
  }
}
